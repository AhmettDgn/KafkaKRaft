apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lab.name" . }}-scripts
  labels:
    {{- include "lab.labels" . | nindent 4 }}
data:
  start.sh: |
{{ .Files.Get "files/start.sh" | indent 4 }}
